model Gallery {
    id          String          @id
    name        String
    media       Media[]
}

enum MediaType {
    VIDEO
    IMAGE
}

model Media {
    id          String          @id
    url         String
    type        MediaType
    
    gallery_id  String?
    gallery     Gallery?        @relation(fields: [gallery_id], references: [id], onDelete: Cascade)
}
