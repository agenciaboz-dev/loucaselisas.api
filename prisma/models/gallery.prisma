model Gallery {
    id          String          @id
    name        String
    media       Media[]
}

model Media {
    id          String          @id
    url         String
    
    gallery_id  String?
    gallery     Gallery?        @relation(fields: [gallery_id], references: [id], onDelete: Cascade)
}
